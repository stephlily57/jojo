class ProposalGenerator:

    def generate_alternatives(self, context):

        return [
            f"Optimize pricing model for {context}",
            f"Refine supplier scoring for {context}",
            f"Improve execution speed in {context}"
        ]
